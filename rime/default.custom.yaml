patch:
  "menu/page_size": 10
  schema_list:
    - {schema: cangjie6}
    - {schema: double_pinyin}
    - {schema: luna_pinyin}
  "switcher/hotkeys":
    - "Control+F1"
  # 以下爲 emacs-rime 的專屬配置。
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Shift_L: noop
      Shift_R: inline_ascii
      Control_L: noop
      Control_R: noop
      Caps_Lock: commit_code
      Eisu_toggle: clear